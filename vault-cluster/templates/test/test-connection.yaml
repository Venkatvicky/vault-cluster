apiVersion: v1
kind: Pod
metadata:
  name: "{{ include \"vault.fullname\" . }}-test-connection"
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ["wget"]
      args: ["{{ include \"vault.fullname\" . }}:8200"]
  restartPolicy: Never
